const gameBoard = (() => {    
    const gameState = ['', '', '', '', '', '', '', '', ''];


    return {gameState};
})();

const gameController = (() => {
    let gameStatus = true; // True for running, false if done

    const makeAMove = (player) => {
        
    };

    
})();

const playerFactory = () => {
    const player = 'X';
    const bot = 'O';    
};